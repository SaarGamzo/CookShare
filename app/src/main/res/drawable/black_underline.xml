<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color for the underline -->
    <item android:drawable="@android:color/transparent" />
    <!-- Underline shape -->
    <item>
        <shape android:shape="line">
            <stroke
                android:width="2dp"
                android:color="@color/black" />
        </shape>
    </item>
</layer-list>
